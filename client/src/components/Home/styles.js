import {makeStyles} from '@material-ui/core/styles';

export default makeStyles(() => ({
  typeBox: {
    flexDirection: 'row',
    justifyContent: 'space-evenly',
    marginBottom: '.5rem',
  },

  sortBox: {
    flexDirection: 'row',
    justifyContent: 'space-evenly',
    marginBottom: '1rem',
  },

}));
